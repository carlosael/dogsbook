import React from 'react';
import Feed from './Feed.js/Feed';

const Home = () => {
  return (
    <section className='container mainContainer'>
      <Feed />
    </section>
  );
};

export default Home;
